{
    WOPort = PH_SOGO_BIND_ADDRESS:PH_SOGO_BIND_PORT;

    SOGoProfileURL = "mysql://PH_SOGO_DB_USER:PH_SOGO_DB_PASSWD@PH_SQL_SERVER:PH_SQL_SERVER_PORT/PH_SOGO_DB_NAME/PH_SOGO_DB_TABLE_USER_PROFILE";
    OCSFolderInfoURL = "mysql://PH_SOGO_DB_USER:PH_SOGO_DB_PASSWD@PH_SQL_SERVER:PH_SQL_SERVER_PORT/PH_SOGO_DB_NAME/PH_SOGO_DB_TABLE_FOLDER_INFO";
    OCSSessionsFolderURL = "mysql://PH_SOGO_DB_USER:PH_SOGO_DB_PASSWD@PH_SQL_SERVER:PH_SQL_SERVER_PORT/PH_SOGO_DB_NAME/PH_SOGO_DB_TABLE_SESSIONS_FOLDER";

    // Default language in the web interface
    SOGoLanguage = English;

    // Specify which module to show after login: Calendar, Mail, Contacts.
    SOGoLoginModule = Mail;

    // Must login with full email address
    SOGoForceExternalLoginWithEmail = YES;

    // Enable email-based alarms on events and tasks.
    SOGoEnableEMailAlarms = YES;
    OCSEMailAlarmsFolderURL = "mysql://PH_SOGO_DB_USER:PH_SOGO_DB_PASSWD@PH_SQL_SERVER:PH_SQL_SERVER_PORT/PH_SOGO_DB_NAME/PH_SOGO_DB_TABLE_ALARMS";

    // IMAP server
    SOGoIMAPServer = "imaps://PH_IMAP_SERVER:143/?tls=YES";
    //SOGoDraftsFolderName = Drafts;
    //SOGoSentFolderName = Sent;
    //SOGoTrashFolderName = Trash;

    // SMTP server
    SOGoMailingMechanism = smtp;
    SOGoSMTPServer = PH_SMTP_SERVER;
    //SOGoSMTPAuthenticationType = PLAIN;

    // Enable managesieve service
    SOGoSieveServer = sieve://PH_MANAGESIEVE_BIND_HOST:PH_MANAGESIEVE_PORT;
    SOGoSieveScriptsEnabled = YES;
    SOGoVacationEnabled = YES;
    // activate the edition from the preferences window of a forwarding email address.
    SOGoForwardEnabled = YES;

    // Memcached
    SOGoMemcachedHost = PH_MEMCACHED_BIND_HOST;

    //SOGoTimeZone = "Europe/Berlin";

    SOGoFirstDayOfWeek = 1;

    //SOGoRefreshViewCheck = every_5_minutes;
    SOGoMailReplyPlacement = below;

    SOGoAppointmentSendEMailNotifications = YES;
    SOGoFoldersSendEMailNotifications = YES;
    SOGoACLsSendEMailNotifications = YES;

    SOGoPasswordChangeEnabled = YES;

    // Authentication using SQL
    SOGoUserSources = (
        {
            type = sql;
            id = directory;
            viewURL = "mysql://PH_SOGO_DB_USER:PH_SOGO_DB_PASSWD@PH_SQL_SERVER:PH_SQL_SERVER_PORT/PH_SOGO_DB_NAME/PH_SOGO_DB_AUTH_VIEW";
            canAuthenticate = YES;
            isAddressBook = YES;
            userPasswordAlgorithm = md5;
            prependPasswordScheme = YES;
        }
    );

    // Authentication using LDAP
    /*
    SOGoUserSources = (
        {
            type = ldap;

            hostname = ldap://127.0.0.1:389;
            baseDN = "ou=users,dc=acme,dc=com";
            //bindAsCurrentUser = NO;
            bindDN = "uid=sogo,ou=users,dc=acme,dc=com";
            bindPassword = qwerty;
            filter = "objectClass=mailUser AND accountStatus=active AND enabledService=mail";
            scope = SUB;

            // The algorithm used for password encryption when changing
            // passwords without Password Policies enabled.
            // Possible values are: plain, crypt, md5-crypt, ssha.
            userPasswordAlgorithm = ssha;

            CNFieldName = cn;
            IDFieldName = uid;
            UIDFieldName = uid;
            IMAPLoginFieldName = mail;
            //IMAPHostFieldName = mailHost;
            canAuthenticate = YES;
            displayName = "Shared Addresses";
            hostname = "ldap://127.0.0.1:389";
            id = public;
            isAddressBook = YES;
        }
    );
    */
}
